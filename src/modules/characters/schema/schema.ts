import { deleteCharacter } from './deleteCharacter.schema';
import { postCharacter } from './postCharacter.schema';
import { putCharacter } from './putCharacter.schema';

export const SCHEMAS = {
  postCharacter,
  putCharacter,
  deleteCharacter,
};
